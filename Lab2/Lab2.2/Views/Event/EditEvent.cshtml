@model Lab2._2.Models.Event

@{
    ViewBag.Title = "EditEvent";
}

<h2>EditEvent</h2>

@using (Html.BeginForm("EditEvent", "Event", FormMethod.Post))
{

    <div>
        @Html.LabelFor(e => e.name)
        @Html.TextBoxFor(e => e.name)
        @Html.ValidationMessageFor(e => e.name)
    </div>

    <div>
        @Html.LabelFor(e => e.location)
        @Html.TextBoxFor(e => e.location)
        @Html.ValidationMessageFor(e => e.location)
    </div>


    <button type="submit" class="m-2 btn-dark">Save</button>

}

